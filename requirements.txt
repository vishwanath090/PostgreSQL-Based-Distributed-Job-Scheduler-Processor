# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
httptools==0.6.1
uvloop==0.19.0

# Async PostgreSQL driver (no ORM — raw asyncpg for control + performance)
asyncpg==0.29.0

# Validation / serialisation
pydantic==2.8.2
pydantic-settings==2.3.4

# Observability
prometheus-client==0.20.0

# Testing
pytest==8.2.2
pytest-asyncio==0.24.0
httpx==0.27.0          # async test client for FastAPI

# Dev / benchmark
aiohttp==3.9.5         # optional: used in benchmark/load.py
